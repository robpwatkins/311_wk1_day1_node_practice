const split = (str, delim) => {
  return str.split(delim);
}

const pairs = (str) => {
  console.log('heyoo');

}

const reverse = (str) => {
  return str.split('').reverse().join('');

}

module.exports = {
  split,
  pairs,
  reverse
}