const split = (str, delim) => {
  return str.split(delim);
}

const pairs = (str) => {
  // Not working yet

}

const reverse = (str) => {
  return str.split('').reverse().join('');

}

module.exports = {
  split,
  pairs,
  reverse
}